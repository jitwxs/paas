<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jit.edu.paas.mapper.ProjectLogMapper">
    <select id="listProjectLog" parameterType="java.lang.String" resultType="jit.edu.paas.domain.entity.ProjectLog">
        SELECT * FROM `project_log` WHERE `project_id` = #{projectId}
    </select>
</mapper>
